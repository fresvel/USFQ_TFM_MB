# Assets

Coloca aquí los recursos propios del nuevo TFM.

- `figuras/`: imágenes usadas con `\includegraphics`.
- `tablas/`: tablas extensas reutilizables con `longtblr`/`tabularray`.

La plantilla USFQ base no depende de assets externos.
